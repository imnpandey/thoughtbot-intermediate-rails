<%= form_for @text_shout do |form| %>
  <%= form.text_field :body, placeholder: 'Shout content here' %>
  <%= form.submit 'Shout!' %>
<% end %>

<% # whenever you do render on an array,
# it loops throurh that array and for each object
# its going to try to render a partial for it, here our obj is shout so its
# going to look for corresponding partial `partial shouts/_shout`.

# instead of

# @shout.each do |shout
#   render shout
# end
%>

<%= render @shouts %>

